# Design Docs

Figma Makeなどのデザイン案を、実装へ移す前に仕様化するためのドキュメントです。

このディレクトリの内容は、実装コードではなく、デザインレビューと実装用design specを扱います。

## 読む順番

1. `figma-make-scope-review.md`
2. `mvp-implementation-spec.md`
3. `component-breakdown.md`

実装時は、必ず `../product/scope.md` を正とし、このdesign specはMVP範囲から外れないための補助資料として扱います。
