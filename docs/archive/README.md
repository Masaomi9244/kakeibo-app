# Archive

このディレクトリには、旧版、重複、移行済みのドキュメントを置きます。

ここにあるドキュメントは、新規実装や仕様判断の正として扱いません。

必要な内容は、移行先の `product/`、`features/`、`architecture/`、`api/`、`db/`、`standards/` に反映済み、または反映予定です。

---

## ファイル

| ファイル | 扱い |
|---|---|
| `legacy-spec.md` | 旧統合MVP仕様。内容が重複しているため参照用に退避。新規実装の正ではない。 |
